[supervisor]
check_interval_secs = 5
ak_rotation_interval_secs = 3600  # 不设 = 禁用

[[workers]]
name = "mcp-worker-1"
ip = "127.0.0.1"
port = 18888
mcp_address = "https://127.0.0.1:18888/mcp"
description = "本地主 Worker"
auto_restart = true

# remote-control-mcp 主动注册目标（保留空数组表示不注册）
# [[remote_control_mcp]]
# ip = "127.0.0.1"
# port = 18890
# refresh_interval_secs = 60

